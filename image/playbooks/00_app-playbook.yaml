---

- hosts: all
  become: yes
  tasks:
    - name: Update dnf cache and make sure curl, wget, unzip, rsync, git, vim and tmux are installed
      dnf:
        name: "{{ item }}"
        update_cache: yes
      loop:
        - curl
        - wget
        - unzip
        - rsync
        - git
        - vim
        - tmux

